{
  "name": "gradient",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "author": "Santosh Cheethirala",
  "description": "A neural network that trains entirely on your GPU in the browser. The forward pass, backpropagation, and optimizer are WebGPU/WGSL compute shaders, with a live training dashboard and a from-scratch nano-GPT.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@fontsource/archivo": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "@webgpu/types": "^0.1.52",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
